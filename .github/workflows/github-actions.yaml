name: Github Actions PyTests Pipeline
run-name: ${{ github.actor }} is running the tests.
on:
  push:
  workflow_dispatch:

jobs:
  smoke_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.x

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Setup Chrome
        run: sudo apt-get install google-chrome-stable

      - name: Run Smoke Tests
        run: pytest -v -m "login or login_guest"

  test:
    needs: smoke_test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.x

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Setup Chrome
        run: sudo apt-get install google-chrome-stable

      - name: Run Tests
        run: pytest -v -m "not login or not login_guest"
